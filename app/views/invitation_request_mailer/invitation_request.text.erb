<%= t :InvitationRequestBody, :invitation_request_email => @invitation_request.email,
                              :ip_address => @invitation_request.ip_address,
                              :application_name => Rails.application.name,
                              :invitation_request_name => @invitation_request.name,
                              :invitation_request_memo => @invitation_request.memo %>
                              
<%= Rails.application.root_url %>invitations/confirm/<%= @invitation_request.code %>

<%= t :IfNotYouDeleteMessage %>
