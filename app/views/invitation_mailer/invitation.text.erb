<%= t :InvitationEmailBody, :invitation_email => @invitation.email,
                            :username => @invitation.user.username,
                            :application_name => Rails.application.name,
                            :application_url => Rails.application.root_url,
                            :memo => @invitation.memo.present? ? raw("\n\n   #{@invitation.memo}") : "" %>

<%= Rails.application.root_url %>invitations/<%= @invitation.code %>
