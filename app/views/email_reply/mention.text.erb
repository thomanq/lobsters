<%= t :UsernameHasMentionedYouInAComment, :username => @comment.user.username %>

  <%= word_wrap(@comment.plaintext_comment, :line_width => 72).gsub(/\n/, "\n  ") %>

<%= t :ContinueThisDiscussionAtUrl, :url => @comment.url %>
