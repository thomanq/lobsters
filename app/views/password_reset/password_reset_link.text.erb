<%= t :PasswordResetMailBody, :user_email => @user.email,
                              :ip => @ip,
                              :application_name => Rails.application.name %>

<%= Rails.application.root_url %>login/set_new_password?token=<%= @user.password_reset_token %>
