<%= t :BanNotification, :application_name => Rails.application.name, 
                        :banner_username => @banner.username, 
                        :reason => word_wrap(@reason, :line_width => 72).gsub(/\n/, "\n  ") %>